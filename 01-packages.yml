---
- name: Install essential packages
  hosts: all
  become: yes
  tasks:
    - name: Install nginx 
      apt:
        name: nginx
        state: latest
      when: ansible_os_family == "Debian"
    
    - name: Start and enable nginx
      service:
        name: nginx
        state: started
        enabled: yes